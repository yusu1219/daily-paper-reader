---
title: Unsupervised Machine Learning for Adaptive Immune Receptors with immuneML
title_zh: 利用 immuneML 对自适应免疫受体进行无监督机器学习
authors: "Pavlovic, M., Wurtzen, C., Kanduri, C., Mamica, M., Scheffer, L., Lund-Andersen, C., Gubatan, J. M., Ullmann, T., Greiff, V., Sandve, G. K."
date: 2026-04-18
pdf: "https://www.biorxiv.org/content/10.64898/2026.04.15.718648v1.full.pdf"
tags: ["query:gentcr"]
score: 10.0
evidence: 用于AIRR的无监督机器学习、生成建模和蛋白质语言模型嵌入
tldr: 本文介绍了在immuneML平台上扩展无监督机器学习以分析自适应免疫受体数据的新功能，提供统一的聚类与生成模型框架，并结合蛋白语言模型嵌入和可视化方法，有助于发现生物学模式、评估模型鲁棒性及探索潜在混杂因素。
source: biorxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/biorxiv/biorxiv-10-64898-2026-04-15-718648-v1/fig-001.webp\", \"caption\": \"Figure 1 Overview of new immuneML features for dataset exploration and unsupervised machine learning analysis. A. The platform takes AIR(R) dataset and an analysis specification as input, performs the analysis and produces an HTML report as output. New features supporting unsupervised ML analyses include training generative sequence models, clustering analyses, LIgO integration61 for simulation of AIR(R) datasets, extended reporting for exploratory analyses, dimensionality reduction, new embeddings based on protein-language models. B. Focusing on clustering analysis, we illustrate the difference between clustering method, clustering result, and clustering approach. C. The clustering workflow in immuneML consists of three steps: preparation of separate discovery and validation datasets, model selection on discovery data and clustering validation on validation data. Model selection in the second step is based on the stability of the clustering58, validation indices across discovery data subsets59 and visualizations. Validation of the clustering is performed on separate validation data following the framework outlined by Ullmann and colleagues60, offering result-based and method-based validation.\", \"page\": 4, \"index\": 1, \"width\": 979, \"height\": 793}, {\"url\": \"assets/figures/biorxiv/biorxiv-10-64898-2026-04-15-718648-v1/fig-002.webp\", \"caption\": \"Figure 2 Analysis of the sequences produced by different generative models in use case 1. a. The use case overview: from simulating signal-specific sequences using LIgO with 5 different k-mers, to training multiple generative models, to analyzing the generated sequences and comparing them to the original sequences. b. The percentage of signal-specific sequences in the datasets generated by LSTM, VAE, and PWM models out of 6000 generated sequences. The original training dataset with balanced signals is shown for reference. The plot also highlights the fraction of novel sequences vs sequences memorized from the training in the generated datasets. C. The confusion matrix of multi-class logistic regression classifier based on 4-mer frequencies shows that it is possible to distinguish between the sequences of different origin, with PWM-generated sequences standing out the most. d. UMAP visualization based on 4-mer frequencies of the datasets generated by LSTM, VAE, and PWM models, with the original train dataset shown as a reference. The original dataset and LSTM dataset overlap almost completely, while the VAE covers some areas not present in the original dataset. PWM overlaps the least, consistent with its having the lowest percentage of signal-specific sequences.\", \"page\": 6, \"index\": 2, \"width\": 979, \"height\": 558}, {\"url\": \"assets/figures/biorxiv/biorxiv-10-64898-2026-04-15-718648-v1/fig-003.webp\", \"caption\": \"Figure 3 - Clustering results on the IEDB dataset in use case 2. a. Overview of the use case: one experimental dataset for IEDB and four synthetic datasets were analyzed in the same workflow. The dataset was first split into discovery and validation, then clustering analysis was performed on discovery data where one clustering approach was chosen for validation on the validation (held-out) data. b. Adjusted mutual information (AMI) with respect to the epitope labels was computed across discovery data subsets, encodings, clustering methods and number of clusters, where the combination of tcrdist for data representation and hierarchical clustering consistently captured most of the epitope information. c. Stability of different encodings and clustering methods on IEDB discovery data. d. Selected clustering approach (tcrdist-based encoding with hierarchical clustering with 3500 clusters) performs similarly on discovery data and on held-out IEDB dataset for both method-based and result-based validation approaches, as measured by AMI with respect to biological properties (epitope specificity and MHC allele information).\", \"page\": 8, \"index\": 3, \"width\": 979, \"height\": 527}, {\"url\": \"assets/figures/biorxiv/biorxiv-10-64898-2026-04-15-718648-v1/fig-004.webp\", \"caption\": \"Figure 4 - Confounder analysis of an experimental AIRR dataset. a. The dataset consists of 76 Crohn’s disease patients, 67 patients with ulcerative colitis and 23 healthy individuals. The workflow includes initial exploratory analysis, such as label distribution, label overlap, clonotype count distribution, sequence length distribution, gene usage, dataset visualizations and feature comparisons. If necessary, a subsequent (clustering) analysis can be performed if any potential issues were identified in the exploratory analysis. b. Dataset visualization based on positional 3-mer frequencies across repertoires with non-linear principal component analysis colored by batch shows that some batches clearly stand out (S1A, S1B, S2B). None of these batches contain healthy controls. c. Clustering is performed to assess if the batch effects dominate the signal. However, the stability analysis shows that clustering is very unstable. d. Overlap between batch and disease status (Diagnosis1) labels shows that some batches contain exclusively healthy controls (S8A, S8B), exclusively diseased repertoires of either Crohn’s disease or ulcerative colitis, or repertoires with Crohn’s disease only (S7A, S7B). e. Adjusted mutual information measures the similarity between clustering labels across clustering approaches and different metadata labels and indicates that batch effects are consistently detected by all clustering approaches to a different extent. f. Overlap between cluster labels and batch labels for one of the data subsets for the setting that was most correlated with the batch label (3-mer frequencies with k-means with k=14) shows that while some clusters contain the data only from a single batch (e.g., cluster 3 contains only S1B), none of the batches are present in only one of the clusters.\", \"page\": 10, \"index\": 4, \"width\": 979, \"height\": 558}]"
motivation: 由于免疫受体数据大多标注不全，缺乏统一的无监督学习框架限制了模型的稳健性和推广性评估。
method: 在immuneML平台中实现聚类、生成模型、蛋白语言模型嵌入、降维与可视化等统一工作流程。
result: 在三类应用中展示了immuneML在生成模型基准测试、聚类评估和实验数据无监督分析方面的有效性。
conclusion: 该研究为免疫受体领域提供了统一的无监督学习框架，提升了模型的解释性与通用性。
---

## 摘要
机器学习（ML）使得自适应免疫受体库（AIRRs）的分析成为可能，用于生物标志物识别和治疗开发。由于大多数 AIRR 数据仅部分或标注不完备，无监督 ML 对于基序发现、生物学意义上的聚类以及新受体序列的生成至关重要。然而，AIRR 领域尚不存在统一的无监督 ML 框架，这阻碍了模型鲁棒性和泛化能力的评估。在此，我们提出了一个新的 immuneML 版本，以统一的聚类工作流程、可解释的生成模型、与蛋白质语言模型嵌入的整合、降维及可视化等方式推动 AIRR 领域的无监督 ML 发展。我们在三个应用场景中展示了 immuneML 的实用性：(i) 基准测试生成模型在特异性表位序列生成中的性能，评估其特异性与新颖性；(ii) 系统地评估聚类方法在实验受体序列上的表现，并与表位特异性及 MHC 等生物学特征进行对比；(iii) 对实验性 AIRR 数据集进行无监督分析以检测潜在混杂因素——这一实践在相关领域广泛存在，但在 AIRR 分析中尚未探索。

## Abstract
Machine learning (ML) enables adaptive immune receptor repertoires (AIRRs) analyses for biomarker identification and therapeutic development. With the majority of AIRR data partially or imperfectly labeled, unsupervised ML is essential for motif discovery, biologically meaningful clustering, and generation of novel receptor sequences. However, no unified framework for unsupervised ML exists in the AIRR field, hindering the assessment of model robustness and generalizability. Here, we present an immuneML release advancing unsupervised ML in the AIRR field through unified clustering workflows, interpretable generative modeling, integration with protein language model embeddings, dimensionality reduction, and visualization. We demonstrate immuneML's utility in three use cases: (i) benchmarking generative models for epitope-specific sequence generation, assessing specificity and novelty, (ii) systematic evaluation of clustering approaches on experimental receptor sequences against biological properties, such as epitope specificity and MHC, and (iii) unsupervised analysis of an experimental AIRR dataset to examine potential confounding, a practice widespread in related fields but unexplored in AIRR analyses.

---

## 论文详细总结（自动生成）

# 利用 immuneML 对自适应免疫受体进行无监督机器学习 —— 论文详细总结  

---

## 一、核心问题与研究背景  

- **研究动机**：  
  自适应免疫受体库（Adaptive Immune Receptor Repertoires, AIRRs）包含个体数十亿的 T/B 细胞受体序列，是免疫应答和疾病表征的重要信息源。随着数据量的激增，机器学习被广泛用于探索这些受体序列的特异性与免疫反应规律。  
  然而：  
  - 現有 AIRR 数据多为 **标注部分不全或含噪声**；  
  - 当前主流为监督学习，缺乏系统的无监督学习框架。  
  → 在这种情况下，**无监督机器学习（Unsupervised ML）** 对于发现受体结构规律、聚类免疫特征、生成新受体序列等至关重要。  
- **主要问题**：  
  AIRR 领域缺少统一的无监督 ML 平台来：  
  - 支持模型选择与验证；  
  - 衡量方法稳健性与泛化能力；  
  - 集成生成模型与现代蛋白语言模型嵌入。  

**论文总体目标**：在已有 supervised-ML 平台 immuneML 基础上，扩展出一个 **统一的无监督学习框架**，涵盖聚类、生成建模、嵌入、降维和可视化，以改进 AIRR 数据分析的系统性与可重复性。

---

## 二、方法论与技术框架  

- **核心思想**：  
  构建 immuneML 的新版本，使其能：  
  1. 支持无监督学习的工作流（clustering、generative modeling）；  
  2. 提供稳定性分析、内部与外部验证指数；  
  3. 集成丰富的嵌入模型与可视化管线，实现从数据输入到结果解释的闭环。  

- **技术组成**：  

  ### 1. 聚类分析工作流  
  - **三步流程**：  
    1. **准备数据**：将数据分为 “发现集” (discovery) 与 “验证集” (validation)；  
    2. **模型选择**：基于 **稳定性评估（adjusted Rand index）**、内部与外部验证指标（如 silhouette、AMI）来确定最佳聚类策略；  
    3. **模型验证**：采用 Ullmann 等人提出的框架同时执行：  
       - *方法验证（method-based）*：在发现与验证集上独立聚类并对比结果；  
       - *结果验证（result-based）*：以监督方式将发现集聚类标签迁移到验证集。  

  ### 2. 生成模型分析工作流  
  - 集成多种模型：**SoNNia、LSTM、VAE、ProGen2**；  
  - 支持可视化与统计评估，包括：生成序列与原始序列的分布差异、新颖性（memorized vs novel）分析、信号恢复评估。  

  ### 3. 嵌入与表示学习  
  - 新增 **蛋白质语言模型 (Protein Language Models, PLMs)**：ProtT5、TCR-BERT、ESM3；  
  - 整合词向量（Word2Vec）、序列相似、物理化学特征等多维表示。  

  ### 4. 可视化与降维  
  - 内置 **PCA, t-SNE, UMAP** 等方法，用于展示受体序列结构、聚类空间与模型生成效果。  

  ### 5. 程序架构与可扩展性  
  - 基于 **BioNumPy** 的内部数据模型，支持高效数组化运算；  
  - 以 YAML 配置文件定义任务，输出可复现的 HTML 报告与参数说明；  
  - 广泛使用 PyTorch、scikit-learn、transformers 等开源库。  

---

## 三、实验设计与数据集  

### 实验场景  
论文设有 **三个主要应用场景**（use cases）：  

1. **生成模型基准测试**  
   - 数据：通过 immuneML 内置 **LIgO 模拟工具** 生成包含五种已知免疫信号的 TCRβ 序列。  
   - 模型：对比 LSTM、VAE、PWM 三种模型。  
   - 评估指标：信号恢复率、生成序列新颖性、UMAP 可视化与分类器区分度（4-mer frequency 特征）。  

2. **聚类分析评估**  
   - 数据：使用 **IEDB 公共数据库** TCRβ 序列 (~48,000条)，含69个表位标签与MHC信息；另含4个带有已知结构的模拟数据集。  
   - 对比方法：多种嵌入与聚类组合（TCR-BERT、ProtT5、ESMC、k-mer频率、tcrdist + 层次聚类）。  
   - 指标：调整互信息 (AMI)、调整兰德指数 (ARI)、轮廓系数、稳定性分析。  
   - 设置：发现集与验证集各50%划分。  

3. **实验数据中的混杂因素检测**  
   - 数据：IBD队列的单细胞配对TCR/BCR数据（143名病人 + 23名健康对照），包含批次、年龄、性别、诊断等元数据。  
   - 方法：主要分析 BCR 子集，使用 k-mer 编码与 PCA + k-means 聚类，检验批次效应是否干扰结果。  

### Benchmark 和验证策略  
- 通过 **稳定性分析法 (Lange et al.)** 与 **Ullmann验证框架** 对聚类结果进行重复验证；  
- 在生成模型实验中通过 **已知 ground-truth motif** 及模型生成的新序列比例进行效果验证。  

---

## 四、资源与算力  

- 文中 **未明确说明 GPU 型号或训练算力配置**；  
- 仅提及 immuneML 提供高效数据结构 (BioNumPy) 与模块化实现；  
- 推测所用算力属于常规研究级 GPU/CPU，但论文未公开详细硬件参数与训练时长。  

> ✅ 明确说明：算力信息缺失。

---

## 五、实验数量与充分性  

- **总实验结构**：  
  - 3 大 use case × 多组模型与参数组合：  
    - Use case 1：3 种生成模型 × 1 个模拟数据集 + 6000采样序列；  
    - Use case 2：1 实验数据 + 4 模拟数据集 × 多种编码 × 多聚类方法 (≈数十组合)；  
    - Use case 3：1 实验数据 × 多聚类超参数设置。  
- **充分性**：  
  - 模拟数据提供可控的 ground-truth，用于定量验证，方法充分；  
  - 聚类验证包括内部与外部两层，较为系统；  
  - 没有额外的跨平台或消融研究，但三种场景覆盖生成、聚类、偏差分析三类任务，整体较全面。  
- **公平性**：  
  使用相同平台、统一数据格式与评估指标，保证方法对比客观、公平。  

---

## 六、主要结论与发现  

- **免疫受体数据的无监督分析框架首次统一实现**于 immuneML；  
- **生成模型评估**：  
  - LSTM 生成的序列信号最准确但新颖性较低（部分记忆训练数据）；  
  - VAE 生成较多新序列，具更强创新能力；  
  - PWM 性能最弱。  
- **聚类分析结果**：  
  - tcrdist + 层次聚类最能捕获表位与 MHC 信息；  
  - 其 AMI ~0.14，远低于人工模拟数据中的理想值（~0.5–0.8），说明真实生物数据信号较弱、复杂。  
- **混杂因素检测**：  
  - IBD数据中存在批次效应，但总体聚类稳定性低，提示后续监督学习仍可在全数据上进行。  
- **总体贡献**：  
  immuneML 形成从模拟、嵌入、聚类到生成的完整无监督学习生态，提升了分析的可复现性与跨数据集可比性。  

---

## 七、优点与亮点  

- **框架革命性统一**：将多种无监督方法整合入单一平台；  
- **稳健性验证完善**：引入稳定性理论 (ARI/AMI) 与双重验证流程；  
- **可解释与可视化能力强**：支持多种降维方法与丰富报告输出；  
- **开放性与扩展性高**：基于 Python、BioNumPy、PyTorch 架构，易集成新模型；  
- **重视生物学解释**：如可区分表位特异性、人群偏差及序列新颖性分析。  

---

## 八、不足与局限  

- **算力与超参数信息未公开**：无法评估训练资源与可扩展性；  
- **聚类性能相对较低**：真实数据 AMI 较小，说明现有表示难以充分捕捉表位信号；  
- **外部工具兼容性有限**：部分重要聚类软件未纳入（如受限于许可证或语言差异）；  
- **生成模型指标不统一**：缺乏标准化的生物学真实性评估；  
- **数据集范围有限**：主要为模拟与公开数据库，缺乏多样化实验验证（例如时序数据或多物种场景）。  

---

**（完）**
